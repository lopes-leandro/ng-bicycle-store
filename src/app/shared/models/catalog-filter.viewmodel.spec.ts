import { CatalogFilterViewmodel } from './catalog-filter.viewmodel';

describe('CatalogFilterViewmodel', () => {
  it('should create an instance', () => {
    expect(new CatalogFilterViewmodel()).toBeTruthy();
  });
});
